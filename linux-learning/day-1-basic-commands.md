# Linux 学习 Day 1

今天我打开了 `the-book-of-secret-knowledge` 仓库，打算从基础命令开始。

## 学会的 3 个命令
1. `pwd`：显示我当前所在的目录路径。
2. `ls -la`：列出当前目录下所有文件（包括隐藏文件）的详细信息。
3. `cd ~`：快速回到我的用户主目录。

## 踩坑记录
- 一开始忘记加 `-la` 参数，看不到隐藏文件，后来查了手册（`man ls`）才想起来。
